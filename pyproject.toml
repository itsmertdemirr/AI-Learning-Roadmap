[tool.ruff]
line-length = 100
target-version = "py310"
exclude = ["*.ipynb"]

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]  # Bazı örnek dosyalarda açıklayıcı yorumlar 100 karakteri aşabilir

[tool.pytest.ini_options]
testpaths = ["tests"]
